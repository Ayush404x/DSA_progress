#include<iostream>
#include<vector>
using namespace std;
int majorityElement(vector<int>&nums){
    vector<int>ans;
    int frequency=0;
    int store;
    for(int i =0;i<nums.size();i++){
    for(int j=0;j<nums.size();j++){

    }
     if(nums[i] ==nums[j]){ 
        nums[i]=store;
        frequency++;
        }
        else if(store == nums[i]/2)
        nums
    }
}
int main(){
vector<int>nums;
for(int i =0 ; i<nums.size();i++)
    {
        int q;
        cin>>q;
        nums.push_back(q);

    }
}